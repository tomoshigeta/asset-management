# 資産可視化ダッシュボード

自分の資産を棚卸しするための3ページのダッシュボード。
画面構成の設計が完了し、`mock/` にモックがある。

## まず [`SPEC.md`](SPEC.md) を読む

35問の設計セッションで確定した仕様と、**却下した案とその理由**が入っている。
着手前に読むのは次の場合。

- 指標・チャート・ページ構成を追加または変更するとき
- データの持ち方（通貨・期間・粒度）に触れるとき
- 「なぜこうなっているのか」が読み取れない箇所に出会ったとき

ドーナツ・履歴・株価API・利回りは、**意図的に外してある**。
理由は SPEC.md の「却下した案」にある。復活させるには新しい理由が要る。

**総資産・純資産・LTV・不動産の時価は 2026-09 に却下を撤回した**
（金融機関への提出という新しい用途が生じたため）。経緯は SPEC.md
「不動産の時価を入れた」にある。時価は任意入力の推定値で、
**全物件に時価が入っているときだけ**総資産・純資産を出す。この条件を外さないこと。

## このリポジトリの決めごと

- **外部依存ゼロを保つ。** CDN・npm・ビルド・外部APIを使わず、
  `mock/index.html` をダブルクリックすれば動く状態を維持する
- **データは JSON 1枚が正。** 形式は `data/template.json`、項目の説明は `data/README.md`。
  `mock/data.js` は同じ形式のサンプルで、画面は「JSONファイルを読み込む」で本物に切り替える。
  守るべき不変条件は `mock/README.md`
- **入力は Excel（`data/input-sheet.xlsx`）が下書き。** `tools/xlsx_to_json.py` で JSON に変換する。
  シートを直したら `tools/build_input_sheet.py` を実行して作り直し、変換ツールの列位置も合わせる。
  `tools/` は画面の動作に関係しない（外部依存ゼロの制約は `mock/` の話）
- **`recalc.py`（LibreOffice）が通っても Excel で通るとは限らない。**
  `INDEX(範囲, MATCH(範囲, ...))` のような配列渡しは LibreOffice では計算できるが、
  本物の Excel では `#VALUE!` になる。実際に1度これで壊した。
  数式は行ごとの計算に分け、配列を引数に渡さないこと
- **本物の資産データを commit しない。** このリポジトリは public。`data/` は
  白紙のひな形（`template.json` / `input-sheet.xlsx`）と `README.md` 以外を
  `.gitignore` で除外してある。外さないこと。
  **`data/input-sheet.xlsx` は白紙のひな形。記入済みのものを絶対にここへ上書きしない**
- **提出用ページ（`mock/report.html`）は第三者が読む。** 画面の解説はたたんであるが、
  印刷時は `beforeprint` で必ず開く。紙から README は参照できない。
  印刷で色を出すには `print-color-adjust: exact` が要る（既定では棒が白く抜ける）
- **円グラフは提出用ページ（`report.html`）だけ。分類は必ず3つに固定する。**
  却下を撤回できたのは「項目が増えても3つのまま」だからで、
  4分類にすると却下理由（増えると読めなくなる）が戻る。色は検証済みの2組を使う
- **棒は預貯金以外どの行も同じ長さ（各行が100%）。** 最大値基準に戻すと
  小さい行が潰れて損益・返済進捗が読めなくなる。金額は数字の列で読む
- **損益セグメントの斜めハッチは判読性を担保している。** 色覚多様性とダークモードでは
  色だけで区別できないため。色やハッチに触れるときは `dataviz` スキルの
  `scripts/validate_palette.js` で再検証する

## 出発点の資料

`docs/` に最初の手書きスケッチと、最初に作った1枚目のUIがある。
`03-summary.png` / `04-accounts.png` は README に貼っている現在の画面。
**撮り直すときは必ずサンプルデータ（`mock/data.js`）で撮る。**
実データの画面を commit しない。

## 全体像

リポジトリの入口は [`README.md`](README.md)。何のための道具か、
何をやらないか、どう動かすかが書いてある。仕様を変えたら README の
「この道具が『やらない』こと」と「現在地」も見直す。
