# 資産可視化ダッシュボード 確定仕様

2026-09 の設計セッション（35問の grilling）で確定した内容。
**このファイルは「何をどう決めたか、なぜそう決めたか」の記録**です。
モックの動かし方と具体的なスキーマは [`mock/README.md`](mock/README.md) にあります。

---

## 現在地

| フェーズ | 状態 |
|---|---|
| 画面構成の設計 | ✅ 完了（本ファイル） |
| モック作成 | ✅ 完了（`mock/`） |
| データ形式の確定と JSON 読み込み | ✅ 完了（`data/template.json`・`data/README.md`） |
| 入力手段（Excel 入力シート） | ✅ 完了（`data/input-sheet.xlsx`） |
| 実データでの試運転 | 🔄 進行中 |
| 実装 | ⬜ 未着手 |

出発点の資料は [`docs/`](docs/) にあります。

- `01-hand-sketch.pdf` / `.png` — 最初の手書きスケッチ
- `02-first-page-mockup.html` — 最初に作った1枚目のUI（現在のモックの原型）
- `03-summary.png` / `04-accounts.png` — README に貼っている現在の画面
  （サンプルデータ。実データでは撮り直さないこと）

---

## 前提

| 項目 | 決定 |
|---|---|
| 目的 | **棚卸し** — どこに何がいくらあるかを把握する |
| 利用者 | 自分専用。将来は顧客向けアプリへ発展させる |
| データ | 手書きの単一ファイルが正。外部APIは株価・為替とも一切使わない |
| 時間軸 | スナップショットのみ。履歴・前回比は持たない |
| 表示端末 | PC中心。モバイルは折り返しで対応 |
| 個人情報 | 氏名のみ |

### 資産と負債の定義（時価の有無で切り替わる）

**全物件に時価が入っている場合**、資産と負債の範囲が揃うので引き算できます。

- **総資産** ＝ 金融資産（有価証券＋保険の解約返戻金＋預貯金）＋ 不動産（時価）
- **総負債** ＝ 総借入額（不動産ローン＋車・多目的）
- **純資産** ＝ 総資産 − 総負債

**時価が1件でも欠けている場合**、資産側だけが小さくなって範囲が揃いません。
この非対称のまま引き算すると、不動産に投じた自己資金が資産側から消えたまま
負債だけが引かれ、**純資産が実態より小さく出ます**。
よってその場合は純資産を出さず、金融資産と総負債を並置し、欠けている件数を表示します。

初版では時価を持たなかったため常に後者でした。経緯は
「不動産の時価を入れた（2026-09 追記・却下の撤回）」を参照。

---

## ページ構成（3枚）

| # | ページ | 役割 |
|---|---|---|
| ① | サマリー | 金融資産と総負債を並置。合算しない |
| ② | 不動産 | 物件別の投資構成と月次収支 |
| ③ | 資産・負債明細 | 有価証券・保険・預貯金・負債 |
| 提出用 | 印刷・PDF | 上記を1つの文書にまとめた、第三者に渡すための頁 |

③ は当初「金融資産」でしたが、負債を同居させたため改名しました。

### ① サマリー

- 左上に氏名と基準日
- **金融資産合計** と **総負債** を並置（引き算しない）
- その下に不動産ブロックと金融ブロックを並置（合算しない）

### ② 不動産

- 物件ごとに1行の積み上げ横棒 — `自己資金 / 累計返済額 / 残債`
- 全体KPI: 総投資額・借入総額・残債合計・**返済進捗率**
- 月次収支 — `家賃収入 − ローン返済 − 経費（管理費・修繕積立・固定資産税）`
- 物件詳細テーブル — **行が物件、列が属性**
- 時価を入れた物件には含み損益と LTV を出す（時価は任意入力の推定値）

### ③ 資産・負債明細

- **有価証券** — 国内・海外とも `株 / 債券 / 投資信託 / その他` の4行を常に表示。
  「その他」は暗号資産・金・銀などの現物資産をまとめる器
- **保険** — 貯蓄性のある契約のみ。評価額は解約返戻金
- **預貯金** — 金融機関別。棒の長さがそのまま構成比
- **負債** — ローン単位。車・多目的の明細に加え、
  「不動産ローン 合計」を1行だけ再掲し、このページで総負債が閉じるようにする

---

## データモデルの原則

具体的なスキーマは [`data/template.json`](data/template.json)（ひな形）と
[`data/README.md`](data/README.md)（項目の説明）を参照。
`formatVersion: 1` として固定した。項目を足す・変えるときは版を上げる。
設計上の決めごとは以下。

- **手書きの単一ファイルが正。** 複数の入力元を正にすると、どれが最新か分からなくなる
- **期間を持つ金額はすべて月額。** 固定資産税は年額を12で割った平均月額
- **有価証券は評価額ベース。** 元通貨で取得原価と評価額を持ち、
  為替レートはデータファイルに手書きして固定する
- **負債はローン単位。** 金融機関別に持つと個別ローンへ分解できない（逆は可能）
- **借入期間は月数で持つ。** 小数の年（26.9166…）だと端数が何か月なのか復元できない。
  入力は「年」「か月」の2欄、保持は `termMonths`、表示は「26年11か月」
- **保険の評価額は解約返戻金。** 他の資産と同じ「今これを現金にしたらいくらか」に
  答えられる唯一の金額。死亡保険金額は参考表示のみで資産に足さない
- **画面は JSON を読み込むだけで、書き換えない。** `file://` のまま動かすため
  ファイル選択で読み、ブラウザに控えを置く。正は手元の JSON ファイルのまま
- **入力は Excel、正は JSON。** 手で JSON を書くのが負担だったため、入力シート
  （`data/input-sheet.xlsx`）を下書きとして用意し、`tools/xlsx_to_json.py` で変換する。
  Excel は下書きであって正ではない。「スプレッドシートを正とする」案（却下済み）とは別物
- **本物のデータは commit しない。** リポジトリが public のため `data/` を
  `.gitignore` で除外。氏名と資産全額が1ファイルに揃うことは顧客向け版の設計要件でもある

---

## 色ルール

> **塗られた面 ＝ 何であるか。文字の色 ＝ 良いか悪いか。
> ベタ塗り ＝ 元本・カテゴリ。斜めハッチ ＝ 損益。**

カテゴリ色は3つだけで、意味は3ページ共通です。

| 色 | 意味 |
|---|---|
| 緑 | 自分の持ち分（自己資金・元本・預金残高） |
| 紫 | 返済済み（借入のうち返し終わった分） |
| 青 | 負債（残債） |

損益は日本式（赤＝益、青＝損）。`▲` は会計慣行どおりマイナスを表します。
具体的な hex 値は [`mock/README.md`](mock/README.md) にあります。

### ハッチは装飾ではありません

損益を面にも塗るため、**色に依存しない2つ目の手がかり**が必須です。
機械検証で以下が出ています。

| 検証項目 | 結果 |
|---|---|
| 赤と緑の隣接（ダーク） | ΔE 6.3 — 要注意帯。色覚多様性で最も危険な組み合わせ |
| 含み損の青 vs 残債の青（ダーク） | ΔE 12.8 — **下限15を下回り判別不能** |

**ハッチを外すと、ダークモードと色覚多様性で読めなくなります。**
色を変更するときは必ず再検証してください（`dataviz` スキルの
`scripts/validate_palette.js`）。

---

## 却下した案と、その理由

**このセクションが本ファイルで最も重要です。**
以下はいずれも検討の末に外したものです。復活させるには新しい理由が必要です。

| 却下したもの | 理由 |
|---|---|
| ~~**総資産**~~ | ~~不動産に時価がなく合算が不可能~~ → **時価が入ったため撤回**（全物件に時価があるときのみ表示） |
| ~~**純資産**（資産 − 負債）~~ | ~~資産に不動産を含めず負債だけ含めるため実態より小さく出る~~ → **撤回。ただし却下理由を仕組みで潰した**（全物件に時価が揃うまで出さない） |
| ~~**LTV**~~ | ~~分母である物件価値が確定できず計算不能~~ → **時価が分母になったため撤回**。返済進捗率も併存 |
| ~~**不動産の時価・含み益**~~ | ~~時価が一意に決まらない~~ → **2026-09 に撤回。任意入力で復活** |
| **利回り（表面・実質）** | 経費を持つため計算はできる。しかし画面に出すと「利回りの高い物件が良い物件」という読み方を誘発し、棚卸しという目的から外れる |
| **履歴・前回比** | 知りたいのは取得原価に対する含み益であって前回比ではない。これにより履歴データが不要になった |
| ~~**円グラフ**~~ | ~~口座や借入先が増えると急速に読めなくなる~~ → **2026-09 に撤回。分類を3つに固定して提出用ページにのみ採用**（下記） |
| **ドーナツチャート**（物件カード） | 複数並ぶと角度の比較になり、物件間の大小が読めない。積み上げ横棒に統一 |
| **物件＝列のテーブル** | 物件が増えると横スクロールでしか読めない。行＝物件に転置した |
| **有価証券の数量ベース保持** | 株価APIが必要になる。取得に失敗すると画面が真っ白になる |
| **為替レートのAPI取得** | 過去のデータを開いたとき当時と違うレートで再計算され、記録として一貫しない |
| **証券会社のスクリーンショットからの入力** | OCRの誤読が静かに混入する。桁を1つ間違えても画面は正常に描画される |
| **CSV／スプレッドシートを正とする** | 銀行別・クラス別という入れ子構造を平坦化すると列が爆発する。<br>**入力の下書きとしての Excel は別途採用した**（下記） |
| **経費に保険料を含める** | 初版では管理費・修繕積立・固定資産税の3項目に限定。含めない旨を明記する |
| **グレー `#E5E7EB` をカテゴリ色に使う** | 彩度・明度帯・コントラストの3項目で検証FAIL。紫に差し替えた |
| **「収支レポート」ナビ項目** | 中身を作らないため。動かないナビゲーションは、無いより悪い |

### 覆した決定

議論の途中で一度決めた後、後から覆したものです。経緯を残します。

| 決定 | 当初 | 最終 | 覆した理由 |
|---|---|---|---|
| 前回比 | スナップショット＋前回比 | **含み益のみ**（前回比なし） | 見たいのは取得原価に対する損益だった。結果として履歴が不要になった |
| 損益の表現 | 損益は文字の色だけ。面には塗らない | **面にも塗る（斜めハッチ付き）** | 取得原価を細い縦線で示す方式は、線の位置を目測させることになり読めなかった。面積にすれば一目。ハッチを足すことで「面の色だけに意味を負わせない」という当初の狙いは形を変えて維持した |
| 負債の棒の縮尺 | 最大ローンを基準 | **各ローンの当初借入額＝100%** | 車・多目的ローンが不動産ローンの前で潰れ、返済進捗が読めなかった。規模差は当初借入額の列が担う |
| 預貯金の棒の縮尺 | 最大口座を基準 | **預貯金合計を基準** | 棒（最大値基準）と構成比（合計基準）が同じ行に並び、分母が2つあって読めなかった |

---

## 不動産の時価を入れた（2026-09 追記・却下の撤回）

**当初「不動産の時価・含み益」「総資産」「純資産」「LTV」を却下していたが、撤回した。**

### 撤回の理由

却下は「自分専用の棚卸し」という前提の下での判断だった。
実データを入れて運用したところ、**金融機関へ提出する資料としても使う**ことになった。
融資審査では担保評価と純資産が中心的な判断材料であり、時価の欠けた資料は用をなさない。
利用者が変われば要件も変わる。これが新しい理由にあたる。

### 当初の反対理由は消えていない

「時価が一意に決まらない」という指摘そのものは正しい。だから**精度を装わない設計**にした。

| 決定 | 理由 |
|---|---|
| 時価は**任意入力**。物件ごとに入れる／入れないを選べる | 査定を取っていない物件に数字をでっち上げないため |
| `valuationBasis`（根拠）と `valuedAt`（評価日）を持てる（**任意**） | 半年後に「この数字は何だったか」を辿れるようにするため。提出先は必ず根拠を聞く |
| 画面にも紙にも「**保有者が入力した推定値**」と明記 | 査定書のような確定値と誤解されないため |
| **総資産・純資産は、全物件に時価が入っているときだけ出す** | 1件でも欠けたまま引き算すると資産側だけ小さくなり、純資産を過小評価する。これは当初 純資産を却下した理由そのもの。欠けているときは件数を明示して数字を出さない |

**LTV** は分母（時価）ができたため計算可能になった。返済進捗率は別の指標として残す。

**利回り**は却下したまま。「利回りの高い物件が良い物件」という読み方を誘発するという
理由は、提出用途が加わっても変わらない。必要になったら改めて議論する。

## 解説の出し分け（2026-09 追記）

画面の解説は、**毎月見る本人には冗長で、初めて見る第三者には必要**という
相反する要求がある。読み手で分けることにした。

| 種類 | 扱い |
|---|---|
| 判断を誤らせないための囲み（引き算しない理由・保障の対価など） | **常に表示**。短く、これが無いと数字を読み違える |
| 表の読み方の説明 | **たたむ**（「▶ この表の読み方」）。開閉はブラウザが記憶 |
| 印刷（＝提出）時 | **すべて自動で開く**。提出先は README を読めない |

閉じた `<details>` の中身は CSS では表示できない（ブラウザが内部で隠す）ため、
`beforeprint` で開閉そのものを切り替えている。

## 提出用ページ（2026-09 追記）

金融機関への提出・第三者への送付のために `mock/report.html` を追加した。

- 3ページの内容を**1つの文書**に連結。⌘P →「PDFに保存」で1つの PDF になる
- 表紙に氏名・基準日・**作成日**・為替レート
- 物件の表は「物件と評価」「物件ごとの借入」の**2枚に分ける**
  （14列を1枚に並べると A4 縦で右端が切れるため）
- 末尾に注記 —「**本書は保有者が作成した一覧であり、残高証明書等の証憑ではない**」
- `@media print` で、サイドバーとボタンを外し、`print-color-adjust: exact` で棒を必ず刷る
  （既定では背景色が印刷されず、棒がすべて白く抜ける）

**送るのは PDF。Excel と JSON は送らない。**
Excel には検算タブや見本行など相手に不要なものが入り、受け取った側が書き換えられる。

## 円グラフを提出用ページにだけ入れた（2026-09 追記・却下の撤回）

**円グラフの却下を撤回した。** ただし提出用ページ（`report.html`）の1枚目に限る。

### 撤回できた理由

却下の理由は「**口座や借入先が増えると急速に読めなくなる**」だった。
今回入れるのは**分類を3つに固定した**円グラフなので、項目が増えても3つのまま
であり、却下の理由そのものが当たらない。用途も違う — 金額を読み取るためではなく、
第三者が**ポートフォリオの構成を一目で掴む**ためのもので、金額は隣の凡例で読む。

| | 3分類 |
|---|---|
| 総資産 | 不動産（時価）／ 有価証券 ／ 預貯金・保険 |
| 総負債 | 不動産ローン ／ 多目的・その他ローン ／ 自動車ローン |

**常に3つに保つ仕掛け**を入れてある。

- 不動産の時価が未入力なら、資産側は「有価証券／保険／預貯金」に切り替え、
  見出しを「金融資産の内訳」に変えて時価が無い旨を出す
- 保険は、不動産があるときは「預貯金・保険」にまとめて3つに収める
- 借入は車以外をすべて「多目的・その他」に寄せる。借入先が増えても3つのまま
- **中身が2種類以下のときは円にしない**（1スライスの円には情報がないため、
  金額を並べた行に替える）

### 色

`dataviz` スキルの `scripts/validate_palette.js` で検証済み。

| | 色 |
|---|---|
| 資産 | 藍 `#2474cf`（不動産）／ 菫 `#9752b4`（有価証券）／ 翡翠 `#17a478`（預貯金・保険） |
| 負債 | 藍 `#2474cf`（不動産ローン）／ 青緑 `#00a58f`（多目的・その他）／ 薔薇 `#b2468a`（自動車） |

ライト・ダーク両モードで**全ペア**を検査し、明度帯・彩度・色覚多様性の分離・
通常視の分離・背景とのコントラストのすべてを通過している。
不動産と不動産ローンに同じ藍を当てているのは、**貸借の両側にある同じ対象だから**。

資産側は色覚多様性の分離が 6.0（8未満の注意帯）なので、
**凡例に名前・金額・割合を並べて色以外の手がかりを必ず添える**という条件つきで採用した。
色を変えるときは再検証すること。

### 暖色をやめた経緯

初版は 琥珀 `#bb6802` を不動産に当てていたが、**金融書類として濁って見える**と
指摘を受けて差し替えた。原因は明度帯の制約（ライト L 0.43–0.77／ダーク L 0.48–0.67）で、
黄〜橙をこの帯に収めると彩度が落ちて茶色に寄るため。
**暖色を候補から外し、寒色〜紫で組み直した。**
薔薇 `#b2468a` だけ暖色寄りだが、赤（損失の色）とは十分に離れている。

## 棒の長さを全行そろえた（2026-09 追記）

**有価証券・保険・不動産の棒を、行ごとに100%へ正規化した。**

最大値を基準にしていたため、国内株（50万円）が投資信託（3,000万円）の隣で
潰れ、含み損益がまったく読めなかった。**負債の表で一度解決したのと同じ問題**である
（SPEC「覆した決定」の「負債の棒の縮尺」を参照）。同じ解き方を適用した。

| セクション | トラック全体＝100% |
|---|---|
| 有価証券 | その行の `max(取得原価, 評価額)` |
| 保険 | その行の `max(払込保険料, 解約返戻金)` |
| 不動産（投資構成） | その物件の総投資額 |
| 不動産（時価） | その物件の `max(総投資額, 時価)` |
| 負債 | その行の当初借入額（従来どおり） |
| 預貯金 | 預貯金合計（**従来どおり変更しない**） |

棒は「元本に対して損益がどのくらいか」「返済がどこまで進んだか」だけを示す道具になった。
**金額の大小は数字の列で読む。**

預貯金だけ据え置いたのは、あの棒の仕事が構成比そのものだから
（棒の長さ＝全体に占める割合）で、他とは目的が違う。

## 保険の扱い（2026-09 追記）

初回の実データ投入で、変額終身保険の評価額が決まらないという問題が出た。
議論のうえ以下に決めた。

| 論点 | 決定 | 理由 |
|---|---|---|
| 評価額に何を使うか | **解約返戻金** | 他の資産と同じ「今これを現金にしたらいくらか」に答えられる。積立金は解約控除前で、実際に手にできる額とズレる |
| 死亡保険金額 | **参考表示のみ。資産に足さない** | 今使えるお金ではない。足すと総額が実態より大きく出る（総資産を却下したのと同じ理由） |
| 含み損益を出すか | **出す。注記をつける** | 正直な数字を並べ、ズレの理由は文章で説明するという本ダッシュボードの方式に合わせた。ただし払った保険料には保障の対価が含まれるため、投資の損益とは意味が違う旨を画面に明記する |
| 置き場所 | **独立した `insurance` セクション** | 有価証券の「その他」は現物資産の器で定義が合わない。国内/海外の軸も保険には意味をなさない。列も違う（払込保険料・解約返戻金・評価日・死亡保険金額） |
| 解約返戻金が不明のとき | **払込保険料と同額を入れ、注記に「未確認（暫定）」** | 0 にすると棚卸しから抜け落ちる。推測値を入れると静かに間違う（スクショ入力を却下したのと同じ理由）。含み損益は ±0 になる |

保険は独立させたが、**金融資産合計には引き続き含まれる**（それ以前は有価証券の
「その他」に入っていたため、合計額は変わらない）。合計と範囲をそろえるため、
サマリーの「うち含み損益」も有価証券＋保険とした。

## 入力シート（Excel）

JSON を手で書くのが負担だったため、入力の下書きとして
[`data/input-sheet.xlsx`](data/input-sheet.xlsx) を用意した。

**却下した「スプレッドシートを正とする」案とは別物である。**
却下の理由は入れ子構造を1枚の表に平坦化すると列が爆発することだったが、
入力シートはセクションごとにタブを分けており、平坦化していない。
正はあくまで `my-assets.json` で、Excel はその下書きにすぎない。

設計上の決めごと。

- **足し算で出せる数字は Excel の数式に持たせる。**
  総投資額・累計返済額・固定資産税の月額・月次収支は自動計算。
  これで `totalInvestment = ownFunds + principal` などの不変条件が
  入力の時点で必ず成り立ち、検算エラーが原理的に起きない
- **入力欄と自動計算欄を色で分ける。** 薄い黄＝入力、薄い灰＝自動計算
- **「検算」タブが画面と同じ数字を出す。** 送る前に本人が確かめられる
- **変換は `tools/xlsx_to_json.py` が行い、書き写さない。**
  足し算で出る値はツール側で計算し直す（Excel の計算結果を信用しない）
- **`tools/` はダッシュボードの動作に関係しない。** 画面は引き続き外部依存ゼロ
- **時価は残債の隣（I列）に置く。** 初版は固定資産税の後ろ（R列）に置いたため、
  17列ぶん右へスクロールしないと見つからず「そんな欄はない」と言われた。
  純資産を出す鍵の入力を奥に隠さないこと
- **列の多い表は1列目も固定する。** 物件名・保険名を見失わずに横スクロールできる
- **借入期間は「年」「か月」の2欄で受ける。** 合計月数は Excel が計算する。
  「か月」が 0〜11 の外なら検算タブが警告する
- **数式は配列を引数に渡さない。** `INDEX(範囲, MATCH(範囲, ...))` は LibreOffice の
  検証を通るのに本物の Excel では `#VALUE!` になる。初回の記入でこれを踏んだため、
  円換算は行ごとの列（`円換算した取得原価`）に分けた

## 未決事項

次に着手するとき、まずここを確認してください。

- **負債の構成比列** — 棒を各ローン100%にしたため、「不動産ローンが借入の大半」
  という規模感が棒から読めなくなった。構成比の列を足すか検討する
- **経費の保険料** — 火災保険は年払いで金額も大きい。含めるか
- **顧客向け版** — 個人情報の範囲、顧客ごとのデータ分離、保存場所。
  他人の個人情報と資産全額が1ファイルに揃うため、扱いが設計要件になる
- **編集機能** — 「物件追加」ボタンは現在プレースホルダ。
  実装するとデータファイルが唯一の正でなくなるため、保存方式の決定が要る

---

## 次のステップ

実装フェーズ。現在のデータ構造が将来のスキーマの下敷きになります。

1. モックをブラウザで一通り触り、この仕様に違和感がないか確かめる
2. 実データを入れて、想定した粒度で運用が続けられるか試す
   — `data/input-sheet.xlsx` に記入 → `tools/xlsx_to_json.py` で変換 →
   「JSONファイルを読み込む」で映す（手順は `data/README.md`）
3. 上記が通ってから実装に進む
